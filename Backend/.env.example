# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=din@email.com
SMTP_PASSWORD=ditt-app-lösenord
SMTP_ENCRYPTION=tls

# Mail Settings
MAIL_FROM_ADDRESS=noreply@dinapp.se
MAIL_FROM_NAME=Din app

# Database (om du behöver)
DB_HOST=localhost
DB_NAME=training_db
DB_USER=root
DB_PASS=password